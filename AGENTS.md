# Aturan Kerja

Before any task, load graphify skill for codebase structure understanding. Use `graphify query "<question>"` or read `graphify-out/GRAPH_REPORT.md` for architecture context.

Sebelum melakukan tugas apapun (menulis kode, mengubah file, menjawab pertanyaan, atau mengambil tindakan), **WAJIB** gunakan skill yang relevan dari `.agents/skills/`. Cari skill yang cocok dengan tugas, lalu load skill tersebut sebelum mulai bekerja.
